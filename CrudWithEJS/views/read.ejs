<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="mt-10 ml-10">
        <h1 class="text-white text-3xl mb-4">All Users</h1>
        <div class="text-blue-500 mb-10">
            <a href="/">Go back to Home</a>
        </div>
         <div class="users flex gap-3 flex-wrap" >
            <% if(users.length > 0) { %>
                <% users.forEach(function (user) { %>
                    <div class="user max-w-72 p-4 rounded-lg bg-gray-400 ">
                        <div class="w-full h-56 rounded-lg bg-zinc-600 overflow-hidden">
                            <img class="w-full h-full onject-cover" src="<%= user.image %>" alt="User Image">
                        </div>
                            <h3 class="text-xl mt-3 tracking-tighter"><%= user.name  %></h3>
                            <h5 class="text-gray-500"><%= user.email  %></h5>
                            <div class="mt-10 flex gap-14">
                                <a href="/edit/<%= user._id  %>" class="bg-green-500 rounded-md px-3 py-2">Edit User</a>
                                <a href="/delete/<%= user._id  %>" class="bg-red-500 rounded-md px-3 py-2">Delete User</a>
                            </div>
                     </div>
               <% }) %>
            <% } else { %>
                    <h2 class="text-white text-3xl">No Users yet</h2>
                <% } %>
           
         </div>
    </div>
</body>
</html>